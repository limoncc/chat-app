{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the settings window",
  "windows": ["settings"],
  "local": true,
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
